# 核心依赖库（仅保留代码中实际使用的）
streamlit>=1.30.0,<2.0.0
numpy==1.26.4  # 锁定1.x系列稳定版，避免自动升级到2.0+实验版
pandas>=2.2.0   # 升级到支持Python 3.14的版本
matplotlib>=3.7.0
scikit-learn>=1.2.0

# 🔴 临时注释掉 RDKit（Python 3.14 下无可用版本）
# rdkit-pypi==2023.9.6
